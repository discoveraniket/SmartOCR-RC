Act as a JSON extraction engine. Convert the SOURCE TEXT into a single JSON object following the SCHEMA provided.

### SCHEMA:
{
    "category": "string",
    "id": "string",
    "name": "string",
    "mobile": "string"
}

### EXTRACTION RULES:
1. Use ONLY the keys defined in the SCHEMA.
2. Map "Category" to "category".
3. Map "ID" to "id".
4. Map "name_OF THE PERSON" to "name".
5. Map "Mobile" to "mobile".
6. Extract values EXACTLY as they appear. 
7. Output only raw JSON. No preamble, no conversational text, and no markdown blocks.