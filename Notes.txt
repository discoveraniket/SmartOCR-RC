# About the UI reorganization.
1. I want the app to be single window first app.
2. The app will be used in windows desktop environment. win10/win11. So I want the app to look like native.
3. on start up instead of the configuration settings page, I want the "search Ration card" page to load. the configuration settings page now be a button in side panel.
4. separate the "search ration card" from the other database UI (auto process, download ...etc).
5. the database operations should be a separate button on the side panel. clicking it shuld open the tabs UI on main ui area.
6. Overall the ui should look clean, modern, minimalis.